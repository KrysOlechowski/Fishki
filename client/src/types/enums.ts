export enum CardCreateStatus {
  NEW = "NEW",
  PENDING = "PENDING",
  CREATED = "CREATED",
  FAILED = "FAILED",
}

export enum CardDeleteStatus {
  NONE = "NONE",
  DELETED = "DELETED",
  FAILED = "FAILED",
  PENDING = "PENDING",
}
